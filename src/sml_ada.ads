--  SML/Ada -- declarative state machines with minimal run-time overhead.
--
--  Inspired by Boost.SML (https://github.com/boost-ext/sml).  The intent is
--  to describe a state machine concisely and push as much work as possible
--  to compile time -- here through Ada generics and enumeration types -- so
--  that the generated code stays small and fast.
--
--  The machinery lives in child units, e.g. Sml_Ada.State_Machines.

package Sml_Ada
  with Pure
is

end Sml_Ada;
